1.双目加号运算符重载可以使用友元函数和成员函数。
    a)使用成员函数在声明的时候可以定义为一个参数，因为使用this对象与传入参数相加。
    b)友元函数声明则需要定义2个参数，如果定义为一个参数，则是实现的时候无法获取当前对象的私有属性。
2.输出运算符重载必须使用友元函数，第一个参数必须是iostream类型，第二个参数为需要输出的参数，无法使用成员函数的原因是因为成员函数默认第一个参数为this指针。
3.索引运算符操作的对象是目前对象本身，所以第一个参数必须是this类型，因此只能是成员函数重载。